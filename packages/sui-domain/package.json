{
  "name": "@s-ui/domain",
  "version": "0.0.1-beta.0",
  "description": "SDK for creating domains",
  "scripts": {
    "lib": "rm -Rf ./lib && mkdir -p ./lib && babel --presets sui ./src --out-dir ./lib",
    "prepublish": "npm run lib && node scripts/prepublish",
    "postpublish": "node scripts/postpublish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "6.24.1",
    "babel-preset-sui": "1",
    "fs-extra": "4.0.2"
  }
}
